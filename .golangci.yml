linters-settings:
  errcheck:
    check-type-assertions: true
    exclude-functions:
      - fmt.Println
      - fmt.Printf
      - fmt.Print
      - fmt.Fprint
      - fmt.Fprintf
      - fmt.Fprintln
  govet:
    check-shadowing: true
  golint:
    min-confidence: 0.8
  gofmt:
    simplify: true

linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - typecheck
    - unused
    - gofmt
    - misspell

run:
  timeout: 5m
  issues-exit-code: 1
  tests: true
